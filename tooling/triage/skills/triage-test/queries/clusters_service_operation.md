# Clusters Service Operation Logs

This query divulges all logs pertaining to a specific operation, identified with an operation identifier, often shortened to `opid`. This query requires that the `opid=<value>` be passed in as extra context to the query template engine - find this value from logs in the service calling clusters-service API.